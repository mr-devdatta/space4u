# ------------------------------------------------------------------------------
# .env.container - Container-side environment variables
#
# This file is used to pass environment variables directly into the running containers
# via the env_file: section in docker-compose.yml.
# It load variables directly into the container's runtime environment.
#
# Usage:
#   In docker-compose.yml:
#     env_file:
#       - .env.container
#
# Notes:
#   - These variables are available inside the container at runtime.
#   - They do NOT affect variable substitution in docker-compose.yml itself.
#   - Use this file for application-level configuration (e.g., Laravel .env values).
# ------------------------------------------------------------------------------

# MySQL Settings
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=space4u
MYSQL_USER=laravel
MYSQL_PASSWORD=laravel

# PHP settings

# Apache settings
# APACHE_DOCUMENT_ROOT=/var/www/html/public